/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you are using a custom subfolder in Vercel, 
  // keeping the configuration simple often helps Vercel's auto-detection.
};

export default nextConfig;
